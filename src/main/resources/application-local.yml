server:
  port: 20181

spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: ${SENSOR_DB_URL:jdbc:mysql://localhost:3306/ZEROQ_SENSOR?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${SENSOR_DB_USERNAME:root}
    password: ${SENSOR_DB_PASSWORD:1234}
    driver-class-name: com.mysql.cj.jdbc.Driver

sensor:
  mqtt:
    enabled: ${SENSOR_MQTT_ENABLED:false}
